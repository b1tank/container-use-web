[project]
name = "backend"
version = "0.0.1"
description = "Container Use Web API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]
